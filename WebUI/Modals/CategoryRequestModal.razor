@inherits EditModal<CategoryRequest>

<EditModal @ref="_modal" T="CategoryRequest" Data="Data" Title="@Title" CloseButtonTitle="@CloseButtonTitle" OnSubmit="OnSubmit" Class="category-modal">
    <Content>
        <FluentValidationValidator />

        <FormInput ValidationFor="() => Data.Name">
            <FluentTextField @bind-Value="Data.Name">Név:</FluentTextField>
        </FormInput>
    </Content>
</EditModal>

@code {

    private EditModal<CategoryRequest> _modal;

    public override void Open() => _modal.Open();

    public override void Close() => _modal.Close();

}